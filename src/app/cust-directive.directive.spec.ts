import { CustDirectiveDirective } from './cust-directive.directive';

describe('CustDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
